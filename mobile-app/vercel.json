{
  "buildCommand": "npm install && npm run vercel-build",
  "outputDirectory": "web-build",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}